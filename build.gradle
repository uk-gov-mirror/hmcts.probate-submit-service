buildscript {
    dependencies {
        classpath group: 'org.sonarsource.scanner.gradle', name: 'sonarqube-gradle-plugin', version: '2.7'
        classpath("net.serenity-bdd:serenity-gradle-plugin:2.3.31")
        classpath("net.serenity-bdd:serenity-single-page-report:2.3.31")
    }

}


plugins {
    id 'application'
    id 'pmd'
    id 'org.springframework.boot' version '2.3.4.RELEASE'
    id 'uk.gov.hmcts.java' version '0.12.2'
    id 'com.github.ben-manes.versions' version '0.38.0'
    id 'com.gorylenko.gradle-git-properties' version '1.4.21'
    id 'info.solidsoft.pitest' version '1.5.2'
    id "io.spring.dependency-management" version "1.0.11.RELEASE"
    id 'au.com.dius.pact' version '3.5.24'
    id 'jacoco'
}

apply plugin: 'java'
apply plugin: 'org.sonarqube'
apply plugin: 'jacoco'
apply plugin: 'net.serenity-bdd.aggregator'

gitProperties {
    gitPropertiesDir = new File("${project.rootDir}/src/main/resources/uk/gov/hmcts/probate/services/submit")
    keys = ['git.commit.id', 'git.commit.time']
    dateFormat = "yyyy-MM-dd'T'HH:mmZ"
    dateFormatTimeZone = "GMT"
}

group = 'uk.gov.hmcts.probate'
version = "4.1.0"
mainClassName = 'uk.gov.hmcts.probate.SubmitApplication'

sourceCompatibility = 11
targetCompatibility = 11

jacoco {
    toolVersion = '0.8.2'
}

sonarqube {
    properties {
        property "sonar.exclusions", "**/RegistryConfig.java"
        property "sonar.projectName", "probate :: submit service"
        property "sonar.jacoco.reportPath", "${project.buildDir}/jacoco/test.exec"
        property "sonar.host.url", "https://sonar.reform.hmcts.net/"
        property "sonar.pitest.mode", "reuseReport"
        property "sonar.pitest.reportsDirectory", "build/reports/pitest"
    }
}

test.finalizedBy jacocoTestReport

dependencyCheck {
    suppressionFile = "${project.rootDir}/suppressions.xml"
    scanConfigurations = ['compile']
}

sourceSets {
    testFunctional {
        java {
            compileClasspath += main.output
            runtimeClasspath += main.output
            srcDir file('src/functionalTest/java')
        }
        resources.srcDir file('src/functionalTest/resources')
    }

    testSmoke {
        java {
            compileClasspath += main.output
            runtimeClasspath += main.output
            srcDir file('src/smokeTest/java')
        }
        resources.srcDir file('src/smokeTest/resources')
    }
    testContract {
        java {
            compileClasspath += main.output
            runtimeClasspath += main.output
            srcDir file('src/contractTest/java')
        }
        resources.srcDir file('src/contractTest/resources')
    }
    testPact {
        java {
            compileClasspath += main.output
            runtimeClasspath += main.output
            srcDir file('src/pactTest/java')
        }
        resources.srcDir file('src/pactTest/resources')
    }
}

task functional(type: Test) {
    description = "Runs functional Tests"
    testClassesDirs = sourceSets.testFunctional.output.classesDirs
    classpath = sourceSets.testFunctional.runtimeClasspath
    finalizedBy aggregate
}

functional.finalizedBy(aggregate)

serenity {
    reports = ["single-page-html"]
}

task pactTask(type: Test) {
    description = "Runs pact Tests"
    testClassesDirs = sourceSets.testPact.output.classesDirs
    classpath = sourceSets.testPact.runtimeClasspath
}

task smoke(type: Test) {
    description = "Runs Smoke Tests"
    testClassesDirs = sourceSets.testSmoke.output.classesDirs
    classpath = sourceSets.testSmoke.runtimeClasspath
}

pmd {
    toolVersion = "6.3.0"
    ignoreFailures = true
    sourceSets = [sourceSets.main, sourceSets.test, sourceSets.testFunctional, sourceSets.testSmoke]
    reportsDir = file("$project.buildDir/reports/pmd")
    ruleSetFiles = files("config/pmd/ruleset.xml")
}


jacocoTestReport {
    executionData(test)
    reports {
        xml.enabled = true
        csv.enabled = false
        xml.destination = file("${project.buildDir}/reports/jacoco/test/jacocoTestReport.xml")
    }
}

pitest {
    targetClasses = ['uk.gov.hmcts.probate.*']
    threads = 4
    outputFormats = ['XML', 'HTML']
    timestampedReports = false
    mutationThreshold = 50
}

repositories {
    mavenLocal()
    maven { url "https://dl.bintray.com/hmcts/hmcts-maven" }
    maven { url "https://repo.maven.apache.org/maven2" }
    mavenCentral()
    jcenter()
}

def versions = [
        logging                         : '5.1.1',
        serviceAuthProviderClient       : '3.0.0',
        authCheckerLib                  : '2.1.2',
        ccdClient                       : '4.7.5',
        springCloudStarterNetflixHystrix: '2.1.3.RELEASE',
        springBoot                      : '2.3.4.RELEASE',
        springfoxSwagger                : '2.9.2',
        springCloudWiremock             : '1.2.4.RELEASE',
        serenity                        : '2.0.23',
        serenityreporter                : '2.1.4',
        pact_version                    : '3.5.24',
        probateCommons                  : '1.1.2.1',
        feignHttpClient                 : '10.2.3',
        lombok                          : '1.18.8'
]

dependencyManagement {
    dependencies {
        dependency group: 'org.bouncycastle', name: 'bcpkix-jdk15on', version: '1.67'
        // CVE-2018-10237 - Unbounded memory allocation
    }
}

dependencies {
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-web', version: versions.springBoot
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-actuator', version: versions.springBoot
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-thymeleaf', version: versions.springBoot
    compile group: 'org.springframework.retry', name: 'spring-retry', version: '1.3.1'
    compileOnly "org.springframework.boot:spring-boot-configuration-processor"
    compile("com.fasterxml.jackson.core:jackson-databind:2.11.2") {
        force = true
    }
    compile group: 'io.github.openfeign', name: 'feign-httpclient', version: versions.feignHttpClient


    compile group: 'org.springframework.cloud', name: 'spring-cloud-starter-openfeign', version: versions.springCloudStarterNetflixHystrix
    compile group: 'io.springfox', name: 'springfox-swagger-ui', version: versions.springfoxSwagger
    compile group: 'io.springfox', name: 'springfox-swagger2', version: versions.springfoxSwagger

    compile group: 'uk.gov.hmcts.reform', name: 'logging', version: versions.logging
    compile group: 'uk.gov.hmcts.reform', name: 'logging-spring', version: versions.logging
    compile group: 'uk.gov.hmcts.reform', name: 'logging-appinsights', version: versions.logging
    compile group: 'uk.gov.hmcts.reform', name: 'logging-httpcomponents', version: versions.logging
    compile group: 'uk.gov.hmcts.reform.auth', name: 'auth-checker-lib', version: versions.authCheckerLib
    compile group: 'uk.gov.hmcts.reform', name: 'service-auth-provider-client', version: versions.serviceAuthProviderClient
    compile group: 'uk.gov.hmcts.reform', name: 'core-case-data-store-client', version: versions.ccdClient
    compile group: 'uk.gov.hmcts.reform', name: 'properties-volume-spring-boot-starter', version: '0.0.4'
    compile group: 'org.apache.commons', name: 'commons-lang3', version: '3.12.0'
    compile group: 'uk.gov.hmcts.reform.probate', name: 'probate-commons', version: versions.probateCommons

    compile group: 'javax.ws.rs', name: 'javax.ws.rs-api', version: '2.1.1'
    compile group: 'com.google.guava', name: 'guava', version: '30.1-jre'

    compile group: 'org.pitest', name: 'pitest', version: '1.6.3'
    compile 'info.solidsoft.gradle.pitest:gradle-pitest-plugin:1.3.0'
    compile 'org.codehaus.sonar-plugins:sonar-pitest-plugin:0.5'
    compile group: 'com.sun.xml.bind', name: 'jaxb-impl', version: '2.3.1'
    compile group: 'com.sun.xml.bind', name: 'jaxb-core', version: '3.0.0'

    compileOnly group:'org.projectlombok', name: 'lombok', version: versions.lombok
    annotationProcessor group:'org.projectlombok', name:'lombok', version: versions.lombok

    testCompile("org.springframework.boot:spring-boot-starter-test") {
        exclude group: "com.vaadin.external.google", module: "android-json"
    }
    testCompile group: 'org.springframework.boot', name: 'spring-boot-starter-test', version: versions.springBoot
    testCompile group: 'org.springframework.security', name: 'spring-security-test', version: '5.3.5.RELEASE'
    testCompile group: 'org.springframework.cloud', name: 'spring-cloud-contract-wiremock', version: versions.springCloudWiremock
    testCompile("com.fasterxml.jackson.core:jackson-databind:2.12.2") {
        force = true
    }
    testCompile group: 'uk.gov.hmcts.reform', name: 'service-auth-provider-client', version: versions.serviceAuthProviderClient
    testRuntime group: 'uk.gov.hmcts.reform', name: 'service-auth-provider-client', version: versions.serviceAuthProviderClient
    testRuntime group: 'uk.gov.hmcts.reform.auth', name: 'auth-checker-lib', version: versions.authCheckerLib
    testCompile group: 'uk.gov.hmcts.reform.auth', name: 'auth-checker-lib', version: versions.authCheckerLib

    testFunctionalCompile group: 'net.serenity-bdd', name: 'serenity-rest-assured', version: versions.serenity
    testFunctionalCompile group: 'net.serenity-bdd', name: 'serenity-core', version: versions.serenity
    testFunctionalCompile group: 'net.serenity-bdd', name: 'serenity-junit', version: versions.serenity
    testFunctionalCompile group: 'net.serenity-bdd', name: 'serenity-spring', version: versions.serenity
    testFunctionalCompile group: 'net.serenity-bdd', name: 'serenity-single-page-report', version: versions.serenityreporter
    testFunctionalCompile group: 'io.jsonwebtoken', name: 'jjwt', version: '0.9.1'
    testFunctionalCompile group: 'io.rest-assured', name: 'rest-assured', version: '4.2.0'
    testFunctionalCompile("com.fasterxml.jackson.core:jackson-databind:2.11.2") {
        force = true
    }
    testFunctionalCompileOnly group:'org.projectlombok', name: 'lombok', version: versions.lombok
    testFunctionalAnnotationProcessor group:'org.projectlombok', name: 'lombok', version: versions.lombok
    testFunctionalCompile sourceSets.main.runtimeClasspath
    testFunctionalCompile sourceSets.test.runtimeClasspath

    testCompile group: 'io.rest-assured', name: 'rest-assured', version: '4.2.0'
    testCompile("org.junit.jupiter:junit-jupiter-api:5.7.1")
    testRuntime("org.junit.jupiter:junit-jupiter-engine:5.7.1")
    testImplementation('org.junit.jupiter:junit-jupiter-api:5.3.2')
    testSmokeCompile sourceSets.main.runtimeClasspath
    testSmokeCompile sourceSets.test.runtimeClasspath
    testSmokeCompile("com.fasterxml.jackson.core:jackson-databind:2.11.2") {
        force = true
    }

    testContractCompile group: 'net.serenity-bdd', name: 'serenity-core', version: versions.serenity
    testContractCompile group: 'net.serenity-bdd', name: 'serenity-junit', version: versions.serenity
    testContractCompile group: 'net.serenity-bdd', name: 'serenity-rest-assured', version: versions.serenity
    testContractCompile group: 'net.serenity-bdd', name: 'serenity-spring', version: versions.serenity
    testContractCompile group: 'io.jsonwebtoken', name: 'jjwt', version: '0.9.0'
    testContractCompile group: 'io.rest-assured', name: 'rest-assured', version: '4.2.0'
    testContractCompile("com.fasterxml.jackson.core:jackson-databind:2.11.2") {
        force = true
    }
    testContractCompile sourceSets.main.runtimeClasspath
    testContractCompile sourceSets.test.runtimeClasspath

    testPactCompile group: 'net.serenity-bdd', name: 'serenity-core', version: versions.serenity
    testPactCompile group: 'net.serenity-bdd', name: 'serenity-junit', version: versions.serenity
    testPactCompile group: 'net.serenity-bdd', name: 'serenity-rest-assured', version: versions.serenity
    testPactCompile group: 'net.serenity-bdd', name: 'serenity-spring', version: versions.serenity
    testPactCompile group: 'io.jsonwebtoken', name: 'jjwt', version: '0.9.0'
    testPactCompile group: 'org.springframework.boot', name: 'spring-boot-starter-test', version: versions.springBoot
    testPactCompile("org.junit.jupiter:junit-jupiter-api:5.3.2")
    testPactRuntime("org.junit.jupiter:junit-jupiter-engine:5.3.2")
    testPactImplementation('org.junit.jupiter:junit-jupiter-api:5.3.2')
    testPactCompile sourceSets.main.runtimeClasspath
    testPactCompile sourceSets.test.runtimeClasspath

    testCompile group: 'au.com.dius', name: 'pact-jvm-provider-spring_2.12', version: versions.pact_version
    testContractCompile group: 'au.com.dius', name: 'pact-jvm-provider-spring_2.12', version: versions.pact_version
    testContractCompile group: 'au.com.dius', name: 'pact-jvm-consumer-junit5_2.12', version: versions.pact_version
    testContractCompile group: 'au.com.dius', name: 'pact-jvm-consumer-java8_2.12', version: versions.pact_version
    testContractCompile("org.junit.jupiter:junit-jupiter-api:5.3.2")
    testContractRuntime("org.junit.jupiter:junit-jupiter-engine:5.3.2")
    testContractImplementation('org.junit.jupiter:junit-jupiter-api:5.3.2')
    testContractCompileOnly group:'org.projectlombok', name: 'lombok', version: versions.lombok
    testContractAnnotationProcessor group:'org.projectlombok', name: 'lombok', version: versions.lombok
}

tasks.withType(JavaCompile) {
    options.compilerArgs << '-parameters'
}

ext['rest-assured.version'] = '4.2.0'

bootJar {
    archiveName = 'submit-service.jar'
    manifest {

        attributes 'Implementation-Title': project.name,
                'Implementation-Version': project.version
    }
}


task printVersion {
    doLast {
        print project.version
    }
}

run {
    if (debug == 'true') {
        jvmArgs = ['-agentlib:jdwp=transport=dt_socket,server=y,suspend=y,address=5005']
    }
}
