spring:
  cloud:
    propertiesvolume:
      prefixed: false
      paths: /mnt/secrets/probate
      aliases:
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
        s2s-probate-backend: S2S_AUTH_TOTP_SECRET
